# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# Data folder: ignore everything except placeholder
data/*
!data/.gitkeep
!data/sdh-roster-pairs.json

# Media pipeline: keep the tool + reports, drop the heavy fetch caches
media-pipeline/cache/

# Profiles pipeline: keep the tool + output shard, drop caches and deps
profiles-pipeline/cache/
profiles-pipeline/node_modules/
profiles-pipeline/misses.txt
profiles-pipeline/*.log
